[workspace]
resolver = "2"
members = ["crates/core", "crates/cli", "app/src-tauri"]

# Smaller shipped binaries; debug info stays available in debug builds.
[profile.release]
strip = true
lto = "thin"
